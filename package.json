{
  "name": "cdn-file-server",
  "version": "1.0.0",
  "description": "Secure CDN file serving application",
  "main": "dist/index.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "latest"
  }
}